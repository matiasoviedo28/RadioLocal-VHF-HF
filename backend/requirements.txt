# Dependencias del backend.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1

# --- Fase 1 (relieve / terreno) ---
# rasterio trae wheels manylinux con GDAL embebido: NO hace falta GDAL del sistema
# ni una imagen base pesada. rio-cogeo es Python puro sobre rasterio.
rasterio==1.4.3
rio-cogeo==5.4.1
numpy==2.2.1
httpx==0.28.1

# --- Mejor ubicación (búsqueda de punto óptimo sobre un perímetro) ---
# shapely trae GEOS embebido en sus wheels manylinux, igual que rasterio con GDAL:
# no hace falta instalar GEOS del sistema.
shapely==2.0.6
